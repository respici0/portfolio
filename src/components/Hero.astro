<section class="hero">
  <span class="hero-name">Robert Paul Respicio</span>
  <h1>I build things that work well on both sides.</h1>
  <h2>
    The right tools and clean code for the developers, and a great experience
    for the people who use it.
  </h2>
  <div class="hero-cta">
    <a href="#work" class="btn-primary">View my work</a>
    <a href="#" class="btn-secondary">Resume ↓</a>
  </div>
</section>

<style>
  .hero {
    padding: 4.5rem 2rem 3.5rem;
    border-bottom: 0.5px solid var(--border);
    max-width: 720px;
    margin: 0 auto;
  }

  /* .hero-name {
    font-family: var(--font-mono);
    font-size: 36px;
    color: var(--hero-name);
    letter-spacing: 0.06em;
  } */

  .hero-name {
    font-size: 42px;
    font-weight: 700;
    letter-spacing: 0.01em;
    line-height: 1.1;
    color: var(--hero-name);
    margin-bottom: 1.5rem;
    font-family: "Libre Baskerville", serif;
  }

  .hero h1 {
    font-size: 28px;
    font-weight: 500;
    line-height: 1.2;
    letter-spacing: -0.02em;
    margin-bottom: 0.75rem;
    color: #1a1a1a;
  }

  .hero h2 {
    font-size: 24px;
    font-weight: 400;
    line-height: 1.4;
    letter-spacing: -0.02em;
    color: #6b6560;
    margin-bottom: 2rem;
  }
  .hero-cta {
    display: flex;
    gap: 0.75rem;
    align-items: center;
  }

  .btn-primary {
    background: var(--text);
    color: var(--bg);
    font-size: 13px;
    font-family: var(--font-sans);
    padding: 9px 18px;
    border-radius: var(--radius-md);
    border: none;
    cursor: pointer;
    text-decoration: none;
    display: inline-block;
  }

  .btn-secondary {
    background: none;
    color: var(--muted);
    font-size: 13px;
    font-family: var(--font-sans);
    padding: 9px 18px;
    border-radius: var(--radius-md);
    border: 0.5px solid var(--subtle);
    cursor: pointer;
    text-decoration: none;
    display: inline-block;
  }

  .btn-secondary:hover {
    color: var(--text);
    border-color: var(--muted);
  }
</style>
